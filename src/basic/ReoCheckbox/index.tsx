import SingleChecked from './singleChecked';
import ReoCheckbox from './checkbox';
import { IProps, IReoCheckboxOption, ISingleCheckedProps } from './interface';

const SingleCheckbox = SingleChecked;

export {
    SingleCheckbox,
    ReoCheckbox,
    IReoCheckboxOption,
    IProps,
    ISingleCheckedProps
};
